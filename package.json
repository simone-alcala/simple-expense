{
  "name": "simple-expense",
  "version": "1.0.0",
  "description": "Simple Expense - APIs",
  "main": "index.js",
  "scripts": {
    "build": "tsc",
    "start": "node dist/src/server.js",
    "dev": "nodemon src/server.ts",
    "migrate": "prisma migrate dev",
    "migrate:test": "cross-env dotenv -e .env.test npm run prisma:migrate",
    "seed": "prisma db seed",
    "seed:test": "cross-env dotenv -e .env.test prisma db seed"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {

  },
  "devDependencies": {
    
  }
}
